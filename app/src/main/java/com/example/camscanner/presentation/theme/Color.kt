package com.example.camscanner.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Dark theme palette (extracted from design reference) ──────────────────────

// Backgrounds
val BackgroundDark = Color(0xFF0D1F1E)        // Main screen background
val BackgroundMedium = Color(0xFF1A2E2D)      // Card / surface background
val BackgroundElevated = Color(0xFF1F3533)    // Elevated surface (bottom sheet, dialog)
val BackgroundBottomNav = Color(0xFF111E1D)   // Bottom navigation bar

// Primary accent — teal
val TealPrimary = Color(0xFF2DD4BF)           // Primary action, FAB, active icons
val TealPrimaryDark = Color(0xFF1BA89A)       // Pressed state
val TealContainer = Color(0xFF1A3D3A)         // Chip background, subtle teal surface
val TealContainerLight = Color(0xFF234E4A)    // Slightly lighter container

// Text
val TextPrimary = Color(0xFFFFFFFF)           // Main text
val TextSecondary = Color(0xFF8A9E9D)         // Subtitle, metadata
val TextDisabled = Color(0xFF4A5E5D)          // Disabled text
val TextOnPrimary = Color(0xFF0D1F1E)         // Text on teal background

// Borders & dividers
val BorderSubtle = Color(0xFF243534)          // Card border, divider
val BorderMedium = Color(0xFF2E4543)          // Input border

// Status
val StatusError = Color(0xFFFF5252)
val StatusSuccess = Color(0xFF4CAF50)
val StatusWarning = Color(0xFFFFB74D)

// Icons
val IconDefault = Color(0xFF8A9E9D)
val IconActive = Color(0xFF2DD4BF)
val IconOnPrimary = Color(0xFF0D1F1E)

// Camera overlay
val CameraOverlay = Color(0x66000000)
val CameraGuide = Color(0xFF2DD4BF)
val CameraHandle = Color(0xFF2DD4BF)

// Scrim
val Scrim = Color(0xCC0D1F1E)
